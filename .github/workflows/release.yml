name: Release

on:
  release:
    types: [ created ]

permissions:
  contents: write

jobs:
  build:
    name: Build on ${{ matrix.os }}
    strategy:
      matrix:
        os: [ ubuntu-latest, windows-latest, macos-latest ]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup JDK
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21

      - name: Cache Maven packages
        uses: actions/cache@v6
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Build with Maven
        shell: bash
        run: mvn -B clean package --no-transfer-progress -DskipTests

      - name: Resolve version & stage files (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $versionOutput = & mvn -q -DforceStdout 'help:evaluate' -Dexpression='project.version' 2>$null
          $version = $versionOutput.Trim()
          if (-not $version) { throw 'Failed to extract version from Maven' }
          Add-Content -Path $env:GITHUB_ENV -Value "APP_VERSION=$version" -Encoding utf8

          $stagingDir = 'staging'
          if (-not (Test-Path $stagingDir)) { New-Item -ItemType Directory -Path $stagingDir | Out-Null }

          Copy-Item -Path 'target\insightpc.jar' -Destination $stagingDir
          Copy-Item -Path 'target\lib' -Destination $stagingDir -Recurse
          Copy-Item -Path 'LICENSE' -Destination $stagingDir

      - name: Package app image artifact (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: '& scripts\package-app-image.ps1 -StagingDir staging'

      - name: Resolve version & stage files (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          version=$(mvn -q -DforceStdout 'help:evaluate' -Dexpression=project.version 2>/dev/null)
          version=$(echo "$version" | tr -d '\r')
          if [ -z "$version" ]; then echo "Failed to extract version from Maven" >&2; exit 1; fi
          echo "APP_VERSION=$version" >> $GITHUB_ENV

          mkdir -p dist staging
          cp target/insightpc.jar staging/
          cp -r target/lib staging/
          cp LICENSE staging/

      - name: Package app image artifact (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: bash scripts/package-app-image.sh staging

      - name: Resolve version & stage files (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          version=$(mvn -q -DforceStdout 'help:evaluate' -Dexpression=project.version 2>/dev/null)
          version=$(echo "$version" | tr -d '\r')
          if [ -z "$version" ]; then echo "Failed to extract version from Maven" >&2; exit 1; fi
          echo "APP_VERSION=$version" >> $GITHUB_ENV

          mkdir -p dist staging
          cp target/insightpc.jar staging/
          cp -r target/lib staging/
          cp LICENSE staging/

      - name: Package app image artifact (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: bash scripts/package-app-image_mac.sh staging

      - name: Upload Release Assets
        uses: softprops/action-gh-release@v3
        with:
          files: dist/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
